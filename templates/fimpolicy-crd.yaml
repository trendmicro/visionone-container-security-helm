---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: fimpolicies.container-security.trendmicro.com
  labels:
    app.kubernetes.io/name: fimpolicy-trendmicro-container-security
spec:
  group: container-security.trendmicro.com
  names:
    kind: FIMPolicy
    listKind: FIMPolicyList
    plural: fimpolicies
    singular: fimpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FIMPolicy is the Schema for the fimpolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of FIMPolicy
            properties:
              build:
                type: string
              maxFilesPerNode:
                type: integer
              maxFilesPerRule:
                type: integer
              maxFilesPerScan:
                type: integer
              maxHashSize:
                type: integer
              rules:
                items:
                  properties:
                    baseDirs:
                      items:
                        type: string
                      type: array
                    condition:
                      properties:
                        and:
                          items:
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                        not:
                          x-kubernetes-preserve-unknown-fields: true
                        or:
                          items:
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                        select:
                          description: Selector is a way of describing containers
                            that will be subject of a container rule
                          properties:
                            containerPattern:
                              type: string
                            imagePattern:
                              type: string
                            namespacePattern:
                              type: string
                            podPattern:
                              type: string
                            podSelector:
                              type: string
                          type: object
                      type: object
                    excludeMIMEs:
                      items:
                        type: string
                      type: array
                    excludeSubDirs:
                      items:
                        type: string
                      type: array
                    excludes:
                      items:
                        type: string
                      type: array
                    filter:
                      description: filter to certain types of changes, e.g., user/process
                      properties:
                        and:
                          items:
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                        expr:
                          properties:
                            field:
                              type: string
                            op:
                              type: string
                            value:
                              anyOf:
                              - type: integer
                              - type: string
                              x-kubernetes-int-or-string: true
                          type: object
                        not:
                          x-kubernetes-preserve-unknown-fields: true
                        or:
                          items:
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    group:
                      description: if specified then this rule is an assignment condition
                        for a group/tree of rules
                      items:
                        x-kubernetes-preserve-unknown-fields: true
                      type: array
                    id:
                      type: string
                    includeMIMEs:
                      items:
                        type: string
                      type: array
                    includeSubDirs:
                      items:
                        type: string
                      type: array
                    includes:
                      items:
                        type: string
                      type: array
                    isHost:
                      description: assignment
                      type: boolean
                    labels:
                      type: string
                    maxFiles:
                      description: overrides policy limit
                      type: integer
                    name:
                      type: string
                    realtime:
                      type: boolean
                    searchDepthLimit:
                      type: integer
                    searchFileLimit:
                      type: integer
                  required:
                  - id
                  type: object
                type: array
              schedule:
                type: string
              searchDepthLimit:
                type: integer
              searchFileLimit:
                type: integer
              settings:
                properties:
                  exclude:
                    items:
                      properties:
                        and:
                          items:
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                        not:
                          x-kubernetes-preserve-unknown-fields: true
                        or:
                          items:
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                        select:
                          description: Selector is a way of describing containers
                            that will be subject of a container rule
                          properties:
                            containerPattern:
                              type: string
                            imagePattern:
                              type: string
                            namespacePattern:
                              type: string
                            podPattern:
                              type: string
                            podSelector:
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              sha1Enabled:
                type: boolean
            type: object
          status:
            description: status defines the observed state of FIMPolicy
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the FIMPolicy resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
    additionalPrinterColumns:
    - name: Policy Build
      type: string
      description: The build version of the FIM policy
      jsonPath: .spec.build
    - name: Schedule
      type: string
      description: The schedule for FIM scans
      jsonPath: .spec.schedule

